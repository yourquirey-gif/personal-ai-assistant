# Backend

Node.js + TypeScript + Express service.

## Environment
Copy `.env.example` to `.env` locally and set secrets there. Never commit the real `.env` file.

The OpenRouter API key will be used only from this backend.
