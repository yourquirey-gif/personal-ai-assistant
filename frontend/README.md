# Frontend

React + TypeScript + Vite frontend for the Personal AI Assistant.

## Local development

```bash
npm install
npm run dev
```

The frontend never receives the OpenRouter secret. It will call the backend API instead.
